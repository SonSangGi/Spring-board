<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mqnic.board.mapper.BoardMapper">
    <select id="getBoards" resultType="Board">
        <![CDATA[
        select * from tbl_board where bno > 0 order by bno desc
        ]]>
    </select>

    <select id="getBoard" resultType="Board">
        select * from tbl_board where bno = #{bno}
    </select>

    <select id="getFisrtBoardNo" resultType="Long">
        select bno
        from (
        select rownum rn, bno
        from (
        select bno from tbl_board order by bno
        ))
        where rn = 1
    </select>


    <sql id="criteria">
        <trim prefix="(" suffix=") AND " prefixOverrides="OR">
            <foreach item='type' collection='typeArr'>
                <trim prefix="OR">
                    <choose>
                        <when test="type == 'T'.toString()w">
                            title like '%' || #{keyword} || '%'
                        </when>
                        <when test="type == 'C'.toString()">
                            content like '%' || #{keyword} || '%'
                        </when>
                        <when test="type == 'W'.toString()">
                            writer like '%' || #{keyword} || '%'
                        </when>
                    </choose>
                </trim>
            </foreach>
        </trim>
    </sql>


    <select id="getBoardsWithPaging" resultType="Board">
        <![CDATA[
            select
                bno, title, content, writer, regdate, updatedate
            from
                (
                select /*+ INDEX_DESC(tbl_board pk_board)*/
                    rownum rn, bno,title, content, writer, regdate, updatedate
                from
                    tbl_board
                where
        ]]>
        <include refid="criteria"></include>
        <![CDATA[
            rownum <= #{pageNum} * #{amount}
                )
            where rn > (#{pageNum} - 1) * #{amount}

        ]]>
    </select>

    <select id="getTotalCount" resultType="int">
        select count(*) from tbl_board
        where
            <include refid="criteria"></include>
            bno > 0
    </select>

    <insert id="insertBoard">
        insert into tbl_board(bno, title, content, writer)
        values(seq_board.nextval,#{title},#{content},#{writer})
    </insert>

    <insert id="insertSelectKeyBoard">
        <selectKey keyProperty="bno" order="BEFORE" resultType="long">
            select seq_board.nextval from dual
        </selectKey>

        insert into tbl_board(bno, title, content, writer)
        values(#{bno},#{title},#{content},#{writer})
    </insert>

    <delete id="deleteBoard">
        delete tbl_board where bno = #{bno}
    </delete>

    <update id="updateBoard">
        update tbl_board
        set title = #{title},
            content = #{content},
            updateDate = sysdate
        where bno = #{bno}
    </update>
</mapper>